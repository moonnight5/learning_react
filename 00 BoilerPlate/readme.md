- react 和 vue 一样的 MVVM
- typescript js的超集 大型项目都是用它写的
- webpack .ts -> awesome-typescript-loader(js) ->  babel -> es5

- .js与.jsx   相当于 .ts与.tsx
  react x 文件， JSX TSX 语法的组件文件
  render(
    return (
      <div />
    )
  )
  html template可以在js里运行
  在js里声明html结构

- react组件声明很灵活
  函数式组件 返回一个jsx就可以了